import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Laptop Price Predictor - ML Regression",
  description: "Prediksi harga laptop dengan Machine Learning menggunakan Linear Regression. Bandingkan spesifikasi dan dapatkan estimasi harga real-time.",
  keywords: ["laptop", "price prediction", "machine learning", "regression", "AI"],
  authors: [{ name: "Muhammad Syukri" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>{children}</body>
    </html>
  );
}
